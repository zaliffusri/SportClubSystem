# Vercel Postgres (set automatically when you add "Vercel Postgres" in the dashboard)
POSTGRES_URL="postgres://..."

# Session signing (required in production)
SESSION_SECRET="your-random-secret-at-least-32-chars"
